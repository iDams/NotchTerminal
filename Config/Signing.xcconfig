// Shared signing configuration for contributors.
// Keep personal Apple team IDs out of version control.
// Each developer can override locally via Signing.local.xcconfig.

#include? "Signing.local.xcconfig"
